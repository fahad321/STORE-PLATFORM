# AEM Configuration
# Copy this file to apps/web/.env.local and fill in your values

# Required: AEM GraphQL endpoint URL
# 
# For AEM SDK Local Author (default):
#   http://localhost:4502/graphql/execute.json/store-platform/homePageByPath
#
# For AEM SDK Local Publish:
#   http://localhost:4503/graphql/execute.json/store-platform/homePageByPath
#
# For AEMaaCS Cloud Service Author:
#   https://author-p[program-id]-e[env-id].adobeaemcloud.com/graphql/execute.json/store-platform/homePageByPath
#
# For AEMaaCS Cloud Service Publish:
#   https://publish-p[program-id]-e[env-id].adobeaemcloud.com/graphql/execute.json/store-platform/homePageByPath
#
# Note: The persisted query path parameter will be added automatically by the app
AEM_ENDPOINT=http://localhost:4502/graphql/execute.json/store-platform/homePageByPath

# Optional: AEM connection mode
# Options: PERSISTED_GET (default) or GRAPHQL_POST
# PERSISTED_GET: Uses GET request with query parameters (recommended for persisted queries)
# GRAPHQL_POST: Uses POST request with GraphQL query in body
AEM_MODE=PERSISTED_GET

# Optional: Authorization header for protected endpoints
# Required for AEMaaCS Cloud Service, optional for local SDK
# Examples:
#   Bearer token: AEM_AUTH_HEADER="Bearer your-token-here"
#   Basic auth: AEM_AUTH_HEADER="Basic base64-encoded-credentials"
#   For local SDK (if needed): AEM_AUTH_HEADER="Basic YWRtaW46YWRtaW4="
AEM_AUTH_HEADER=

